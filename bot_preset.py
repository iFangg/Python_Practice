import discord, discord.ext, random, os, json
from Pchecks import checks
from discord.ext import commands, tasks
from itertools import cycle

bot = commands.Bot(command_prefix = "p!")

```
INSERT CODE HERE
```

bot.run("token")
